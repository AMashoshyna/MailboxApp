describe('newUserInput', function() {
    var controller;
    beforeEach(module('UserData'));
    beforeEach(inject(function(_$componentController_) {
        controller = _$componentController_(newUserInput);
        it('should call UserDataService ')

    }))
})